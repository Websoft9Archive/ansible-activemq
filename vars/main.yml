jdk_version: "8"

init_application:
  activemq:
    username: admin
    password: "admin"
    service_before: activemq.service
    service_after: 
    config_paths: 
      - /opt/apache-activemq/conf/jetty-real.properties
