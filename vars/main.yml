jdk_version: "8"

init_application:
  activemq:
    username: admin
    password: "admin"
    service_before: activemq.service
    service_after: 
    commands: 
      - sudo sed -i "s/{{dbs.username}}:{{dbs.password}}/{{dbs.username}}:$new_password/"  /opt/apache-activemq/conf/jetty-realm.properties
